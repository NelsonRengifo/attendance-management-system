# Routes used by the admin dashboard:

# /status → who is checked in

# /logs → list of all check-ins/out

# /user/<id> → info about a specific user

# any tools an admin needs

# These are separate so only admins interact with them.
